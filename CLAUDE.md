# CLAUDE.md - 기본 지침서

## 언어

- 모든 응답은 **한국어**로 작성한다.
- 코드 내 주석도 한국어로 작성한다.
- 변수명, 함수명 등 코드 식별자는 영어를 사용한다.

## 코드 작성 원칙

### 모듈화

- 모든 기능은 적절한 단위로 나누어 **독립된 모듈**로 분리한다.
- 각 모듈은 명확한 이름과 단일 책임을 가진다.
- 모듈 간 의존성을 최소화하여, 특정 모듈 수정 시 다른 모듈에 영향이 없도록 한다.

### 최소 구현 원칙

- 요청받은 내용**만** 구현한다.
- 임의로 "어울릴 만한" 관련 기능을 추가하지 않는다.
- 변경 범위를 최소한으로, 매우 보수적으로 유지한다.

### 코드 스타일

- Google Style Guide를 기본으로 따른다.
- 린트 규칙을 준수하고, 경고를 무시하지 않는다.
- 네이밍: 의미가 명확한 이름을 사용한다 (약어 지양).
- 함수는 하나의 작업만 수행한다.
- 매직넘버 사용을 금지하고, 상수로 정의한다.
- 불필요한 주석 대신, 코드 자체가 의도를 설명하도록 작성한다.

## 트러블슈팅 원칙

- 오류 발생 시 **근본 원인(root cause)**을 반드시 파악한다.
- 임시 우회(workaround)가 아닌, 근본적인 해결책을 적용한다.
- 수정 후, 같은 오류의 재발을 방지하기 위한 지침을 이 문서에 추가한다.

## 문서 작성 원칙

- md 파일에 불필요한 예시 코드를 남발하지 않는다.
- 토큰 낭비를 최소화하도록 간결하게 작성한다.
- 핵심 내용만 담고, 중복 서술을 피한다.

---

## 프로젝트 구조 (prac/)

| 파일                     | 모듈       | 역할                                                       |
| ------------------------ | ---------- | ---------------------------------------------------------- |
| `TodoItem.swift`         | Models     | 할일 데이터 + 난이도 + 검수상태(ReviewStatus)              |
| `UserProfile.swift`      | Models     | 유저 XP/레벨/뱃지 + 프로필 정보 + BadgeCatalog             |
| `TodoStore.swift`        | ViewModels | 할일 CRUD + 검수 승인/거절 + 로컬 저장                     |
| `RewardManager.swift`    | ViewModels | XP 부여/차감 + 레벨업 + 연속달성 + 뱃지 해금 + 프로필 관리 |
| `GlassModifier.swift`    | Components | 리퀴드 글래스 스타일 + 앱 배경                             |
| `CalendarView.swift`     | Views      | 월간 캘린더 + 날짜 선택 + 년월 피커                        |
| `TodoListView.swift`     | Views      | 할일 목록 + 완료 메모 시트 + 검수상태 표시                 |
| `RewardView.swift`       | Views      | 프로필 편집 + 레벨/XP/뱃지 표시                            |
| `ReviewView.swift`       | Views      | 피어 검수 + 내 검수 현황                                   |
| `RankingView.swift`      | Views      | 랭킹 (현재 목업, 서버 연동 필요)                           |
| `ShopView.swift`         | Views      | 경험치 상점 (XP로 리워드 교환)                             |
| `AchievementTitle.swift` | Models     | 칭호/해시태그 시스템 + TitleStats                          |
| `AuthManager.swift`      | ViewModels | 인증 상태 관리 (로그인/로그아웃/관리자)                    |
| `LoginView.swift`        | Views      | 로그인 + 회원가입 (소셜/이메일)                            |
| `AdminView.swift`        | Views      | 관리자 대시보드 (비밀번호: 4268)                           |
| `SplashView.swift`       | Views      | 앱 실행 시 스플래시 애니메이션                             |
| `ContentView.swift`      | Root       | 스플래시 → 로그인 → 메인 탭 라우팅                         |

### 검수 시스템

- 쉬움: 자동 승인 → 즉시 XP 지급
- 보통/어려움: 완료 시 검수 대기 → 승인 후 XP 지급
- 검수자에게 검수 XP 지급 (보통 5XP, 어려움 10XP)
- 거절 시 코멘트 필수

### 어뷰징 방지 규칙

- 하루 최대 20개 할일 추가
- 할일 제목 최소 2글자
- 생성 후 10초 이내 완료 불가
- 일일 XP 상한 500
- 보통/어려움은 피어 검수 필요 (자기 승인은 로컬 한정)

---

## 트러블슈팅 기록

> 오류를 해결할 때마다 아래에 항목을 추가한다.

(아직 기록 없음)

(맥미니에서 테스트 커밋)
